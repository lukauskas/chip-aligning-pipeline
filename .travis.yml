language: python
addons:
   apt:
       packages:
            - libhdf5-serial-dev
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install: 
  - pip install cython
  - pip install .
# command to run tests
script: nosetests
